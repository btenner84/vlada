{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "next build",
  "routes": [
    {
      "src": "/api/(.*)",
      "methods": ["POST", "GET", "OPTIONS"],
      "dest": "/api/$1"
    }
  ]
} 
